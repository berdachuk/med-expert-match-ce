# M136 — Self-Correcting Structured Output for LLM Calls

- **Milestone:** M136
- **REQ:** REQ-133
- **DEC:** DEC-004, DEC-007
- **Status:** Active
- **Date:** 2026-06-24

## Current Focus

Migrate 5 structured-JSON LLM call sites from manual `.call().content()` + `ObjectMapper.readValue()` to Spring AI 2.0's `StructuredOutputConverter` + `.entity()` with `validateSchema()` self-correction.

## Tasks

1. [ ] Create `LenientJsonOutputConverter` in `core/util` with fence-stripping + `BeanOutputConverter` delegation.
2. [ ] Create structured output records (`CaseAnalysisJson`, `GoalClassificationJson`) in owning modules.
3. [ ] Migrate `CaseAnalysisServiceImpl` 4 call sites to `.entity()` with `validateSchema()`.
4. [ ] Migrate `GoalClassifier.classifyByLlm()` to `.entity()` with `validateSchema()`.
5. [ ] Strip JSON format instructions from prompt templates.
6. [ ] Deprecate `LlmResponseSanitizer.extractJson()`.
7. [ ] Write unit tests for `LenientJsonOutputConverter`.
8. [ ] Update `CaseAnalysisServiceIT` for new `.entity()` path.
9. [ ] Run `mvn verify`, fix failures.
10. [ ] Security review.
11. [ ] Run `sync-memory-index.sh --check`.
12. [ ] Commit + merge to develop.

## Open Questions

- None.

## Risks

- RISK-136: Converter edge case coverage — delegate to `extractJson()` logic, not just fence-stripping.
- RISK-137: Retry cost increase — default 3 retries; monitor via `LlmUsageTelemetryService`.
