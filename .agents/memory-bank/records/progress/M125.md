# M125 — Main Menu Restructure

- **Milestone:** M125
- **REQ:** REQ-125
- **Date completed:** 2026-06-19
- **Branch:** feat/m124-m125-main-menu-restructure-and-perf (merged to develop, archived)

## Summary

AI Chat is now the primary entry point at `/`. Removed `HomeController`, `index.html`, dashboard stats. Rewrote header nav: sub-page links always visible, Home→AI Chat at root, back arrow now gates on `currentPage != 'chat'`. Deleted `HomeControllerIT`, added root page test to `ChatWebControllerIT`. Fixed pre-existing `@MockBean` compilation error in `SessionTokenApiKeyAuthFilterIT` (replaced with `@TestConfiguration` + `mock()`).

## Tests

- 938 unit + 567 IT tests pass

## Modules touched

- `web`, `system` (test config)