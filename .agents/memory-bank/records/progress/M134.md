# M134 — Memory-Bank Migration Completion & CI Integration

- **Milestone:** M134
- **DEC:** DEC-015
- **Date completed:** 2026-06-21
- **Branch:** feat/m134-memory-bank-migration-completion (merged to develop, deleted)

## Summary

Completed the migration to the partitioned memory-bank structure and wired the CI gate. Migrated 117 archived plans to `records/progress/` stubs (125 total records). Seeded `nfr.jsonl` (22 NFRs) and `task.jsonl` (16 tasks). Updated `docs/ai-context-strategy.md` with the partitioned layer model. Fixed `sorted_glob_desc` bug (glob expansion truncated to first match).

## What was built

- CI gate: `sync-memory-index.sh --check` added to `.github/workflows/ci.yml` with `jq` install step
- 117 archived plans migrated to `records/progress/M{NN}.md` stubs (one-line title + date)
- `registry/nfr.jsonl` seeded with 22 NFRs (mapped from legacy NFR-X.Y to NFR-###)
- `registry/task.jsonl` seeded with 16 tasks from M133/M134
- `docs/ai-context-strategy.md` updated with partitioned layer model + sync rules
- RISK-134 appended (CI jq dependency, mitigated)

## Bugs fixed

- `sorted_glob_desc`: `for f in "$1"` → `for f in "$@"` (glob expansion was truncated to first match; archive table showed only 1 row instead of 125)
- Stub description extraction: restructured stubs so first content line is the title (was showing `- **Milestone:** M99` instead of the actual title)

## Verification

- `sync-memory-index.sh --check` returns exit 0
- `plans/00-index.md` Archive table lists all 125 milestones from records
- `progress.md` shows 125 milestone entries
- Idempotent

## Traceability

- DEC-015 (extended)
- RISK-134 — mitigated
