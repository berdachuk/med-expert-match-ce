# M140 — Self-Correcting Structured Output (Spring AI 2.0 Phase 2)

- **Milestone:** M140
- **REQ:** REQ-133
- **Status:** Complete
- **Date:** 2026-07-05
- **Completed:** 2026-07-05

## Summary

Migrated five structured LLM call sites from manual `.content()` parsing to Spring AI 2.0 `.entity(..., validateSchema())` via shared `StructuredOutputSupport`, with optional gated `useProviderStructuredOutput()`.

## Deliverables

- `StructuredOutputSupport`, `LlmStructuredOutputProperties`, `StructuredOutputValidationTracker`
- Domain records: `StringListJson`, `UrgencyClassificationJson`
- Migrated: `CaseAnalysisServiceImpl` (4 methods), `GoalClassifier.classifyByLlm()`
- Prompt cleanup: icd10, urgency, specialty, goal-classification templates
- Docs: `MODEL_SELECTION_GUIDE.md` structured-output section
- Keyword fixes: `match doctors?`, `find specialists?` in `GoalIntentPatterns`

## Verification

- `StructuredOutputSupportTest`, `StructuredOutputDomainRecordsTest` pass
- `CaseAnalysisServiceImplTest`, `GoalClassifier*Test`, `ChatTurnContinuityE2ETest` pass
- Full `mvn test`: 985 run; 6 pre-existing failures/errors (Windows classpath / unrelated)

## Security

- Retry loop uses Spring AI schema validation only; no custom PHI appended to prompts
- `provider-native-enabled=false` by default; local Ollama excluded when enabled

## Risks

- RISK-137 — retry cost (metrics wired via M139 `StructuredOutputValidationMetrics`)
- RISK-143 — provider-native gated off for localhost/Ollama
