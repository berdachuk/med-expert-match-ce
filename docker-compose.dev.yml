services:
  postgres-dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    image: medexpertmatch-postgres-dev:latest
    container_name: medexpertmatch-postgres-dev
    environment:
      POSTGRES_USER: medexpertmatch
      POSTGRES_PASSWORD: medexpertmatch
      POSTGRES_DB: medexpertmatch
    ports:
      - "5434:5432"  # Host 5434 to avoid conflict with main stack (5433)
    volumes:
      # Named volume works on Windows, Linux, and macOS (bind to ~/data is unreliable on Windows Docker)
      - medexpertmatch-postgres-dev-data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U medexpertmatch" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medexpertmatch-network

networks:
  medexpertmatch-network:
    driver: bridge

volumes:
  medexpertmatch-postgres-dev-data:
