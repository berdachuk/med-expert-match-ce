/**
 * Chat module — per-user AI chat sessions and message history.
 */
@org.springframework.modulith.ApplicationModule(allowedDependencies = {"core :: *"})
package com.berdachuk.medexpertmatch.chat;
